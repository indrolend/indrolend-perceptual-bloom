{
  "name": "perceptual-bloom-root",
  "private": true,
  "scripts": {
    "dev": "pnpm -C apps/web dev",
    "build": "pnpm -C apps/web build"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
